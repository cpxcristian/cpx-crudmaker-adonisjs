import vine from '@vinejs/vine'

export const {{ modelName }}Validator = vine.compile(
  vine.object({
    {{ columns }}
  })
)

export const {{ modelName }}ValidatorUpdate = vine.compile(
  vine.object({
    {{ columnsUpdate }}
  })
)
